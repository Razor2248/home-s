# Giai đoạn 3 — kết nối frontend với backend NestJS
# Bỏ dấu # để app MẶC ĐỊNH chạy với Server API; nếu để trống, app chạy chế độ Demo (localStorage).
# Địa chỉ cũng có thể đổi ngay trên màn hình đăng nhập mà không cần build lại.
# VITE_API_URL=http://localhost:3001/api/v1
